{
  "name": "cookbook-e2e",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install-browsers": "npx playwright install --with-deps",
    "test": "npx playwright test --reporter=list",
    "report": "npx playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.1",
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.5.2",
    "axe-core": "^4.11.2"
  }
}
